package controller;

import model.game.tower.Tower;

/**
 * @author Sebastian, Samuel
 * The state of the controller.
 * Is used by controller.
 */
public class ControllerState {
    static Class<?extends Tower> selectedTower;
}
